[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "LeJEPA_Cotton_Field_Assessment"
version = "0.1.0"
description = "This package contains code for the pretraining and evaluation of a YOLOV8 for two prediction tasks, namely cotton boll detection and plot status classification."
readme = "README.md"
authors = [
  {name = "Akintan Oreofeoluwa Anuoluwapo", email = "oreofeakintan@gmail.com"}
]
maintainers = [
  {name = "Akintan Oreofeoluwa Anuoluwapo", email = "oreofeakintan@gmail.com"}
]
classifiers = [
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Typing :: Typed",
]
license = {text = "MIT"}
dependencies = [
  "typer",
  "rich",
]
requires-python = ">= 3.12"

[dependency-groups]
dev = [
    { include-group = "lint" },
    { include-group = "test" },
    { include-group = "typecheck" },
]
lint = [
    "ruff",
]
test = [
    "coverage",
    "pytest",
]
typecheck = [
    "ty",
]
docs = [
    "zensical",
    "mkdocstrings-python",
]

[project.urls]
bugs = "https://github.com/Akintanoreofe/LeJEPA_Cotton_Field_Assessment/issues"
changelog = "https://github.com/Akintanoreofe/LeJEPA_Cotton_Field_Assessment/releases"
documentation = "https://Akintanoreofe.github.io/LeJEPA_Cotton_Field_Assessment/"
homepage = "https://github.com/Akintanoreofe/LeJEPA_Cotton_Field_Assessment"

[project.scripts]
lejepa_cotton_field_assessment = "lejepa_cotton_field_assessment.cli:app"

[tool.ty]
# All rules are enabled as "error" by default; no need to specify unless overriding.
# Example override: relax a rule for the entire project (uncomment if needed).
# rules.TY015 = "warn"  # For invalid-argument-type, warn instead of error.

[tool.ruff]
line-length = 120

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # Pyflakes
    "I",   # isort
    "B",   # flake8-bugbear
    "UP",  # pyupgrade
]

[tool.ruff.lint.flake8-bugbear]
extend-immutable-calls = [
    "typer.Argument",
    "typer.Option",
]

[tool.coverage.run]
branch = true
parallel = true
source = ["src/", "tests/"]

[tool.coverage.report]
show_missing = true
skip_covered = true
fail_under = 50
exclude_also = [
    "if TYPE_CHECKING:",
    "if typing.TYPE_CHECKING:",
    "@overload",
    "@typing.overload",
    "class .*\\bProtocol\\):",
    "@(abc\\.)?abstractmethod",
    "raise NotImplementedError",
    "\\.\\.\\.",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.hatch.build.targets.wheel]
packages = ["src/lejepa_cotton_field_assessment"]

[tool.uv]
package = true
default-groups = ["dev"]
